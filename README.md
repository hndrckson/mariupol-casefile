# Mariupol Casefile

Standalone static dashboard prototype for public Mariupol destruction map targets.

## Local Preview

Serve the workspace root and open:

```text
http://127.0.0.1:8765/dashboard/
```

The deployable static app lives in `dashboard/`. Raw captured media is intentionally excluded from Git because the dashboard uses the public hosted source image URLs for the quick web version.
